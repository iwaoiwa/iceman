<%= form_with(model: reminder, local: true) do |form| %>
  <% if reminder.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(reminder.errors.count, "error") %> prohibited this reminder from being saved:</h2>

      <ul>
      <% reminder.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title, id: :reminder_title %>
  </div>

  <div class="field">
    <%= form.label :timing %>
    <%= form.text_field :timing, id: :reminder_timing %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
